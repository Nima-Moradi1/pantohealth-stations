# PANTOhealth — Stations Map

This is a Frontend Assignment by PANTOhealth
Small React + TypeScript app that displays German train stations on a Leaflet map with filtering by city.

## Tech
- React + Vite + TypeScript
- TailwindCSS (design tokens via CSS variables)
- MobX (store for stations/filter/selection + small persistence)
- Leaflet via react-leaflet
- Vitest for tests

## Run locally
```bash
pnpm install
pnpm dev
